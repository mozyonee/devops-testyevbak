name: CI/CD
on:
  push: { branches: [main] }
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  IMAGE: ghcr.io/yevbak/devops-test:latest

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/yevbak/devops-test:latest
            ghcr.io/yevbak/devops-test:${{ github.sha }}

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: helm/kind-action@v1.9.0
        with:
          cluster_name: ci
          wait: true
      - name: Install NGINX Ingress
        run: |
          kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.10.1/deploy/static/provider/kind/deploy.yaml
          kubectl -n ingress-nginx rollout status deploy/ingress-nginx-controller --timeout=180s
      - name: Apply manifests
        run: |
          kubectl apply -f k8s/namespace.yaml
          kubectl -n mustage apply -f k8s/redis-secret.yaml -f k8s/redis-deployment.yaml -f k8s/redis-service.yaml
          kubectl -n mustage apply -f k8s/app-config.yaml -f k8s/app-deployment.yaml -f k8s/app-service.yaml
          kubectl -n mustage apply -f k8s/ingress.yaml || true
      - name: Patch image and rollout
        env:
          IMAGE: ${{ env.IMAGE }}
        run: |
          kubectl -n mustage set image deploy/nest-app nest-app=$IMAGE
          kubectl -n mustage rollout status deploy/nest-app --timeout=180s
